<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/activity_location_suggestion"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:orientation="vertical"
    >

    <TextView
        android:id="@+id/title1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:gravity="center"
        android:letterSpacing="0.5"
        android:text="set_pickup_location_caps"/>

    <TextView
        android:id="@+id/title2"
        android:layout_below="@id/title1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:gravity="center"
        android:textSize="20dp"
        android:textColor="@android:color/background_dark"
        android:text="Help us pick an outlet"/>

    <android.support.v7.widget.RecyclerView
        android:layout_below="@id/title2"
        android:layout_above="@+id/scale_custom_option_list"
        android:id="@+id/scale_recyclerview_rv"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:padding="@dimen/item_spacing"
        android:layout_marginTop="28dp"/>

    <LinearLayout
        android:id="@+id/scale_custom_option_list"
        android:layout_alignParentBottom="true"
        android:layout_marginTop="12dp"
        android:layout_marginBottom="12dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        />
</RelativeLayout>